export function setDocumentClient(state, document) {
  state.DocumentClient = document.data;
}

export function setDocumentCompany(state, company) {
  state.DocumentCompany = company.data;
}

// export function createDocumentCompany(state, document) {
//   state.DocumentClient = document;
// }
//
//
// export function createDocumentClient(state, document) {
//   state.DocumentClient = document;
// }
